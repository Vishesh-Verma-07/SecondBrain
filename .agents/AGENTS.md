# Project Instructions

## Code formatting

- Use Prettier for all formatting.
- Before considering a task complete, run `npm run format`.
- After formatting, run `npm run format:check`.
- Do not manually format code in a way that conflicts with Prettier.
- Follow the existing project Prettier configuration.
